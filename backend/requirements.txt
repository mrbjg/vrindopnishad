# Core web framework
fastapi>=0.109.0
uvicorn>=0.25.0
python-dotenv>=1.0.0
python-multipart>=0.0.6

# Security/Auth
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0
PyJWT>=2.8.0

# Firebase (optional - can be replaced by Supabase)
firebase-admin>=6.2.0

# Supabase (Free alternative to Firebase)
supabase>=2.27.0

# Google Cloud TTS (optional - for audio generation)
google-cloud-texttospeech>=2.14.0

# CORS
starlette>=0.35.0
